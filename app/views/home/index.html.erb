<!DOCTYPE html>
<html>

	<head>
	</head>
	<body font-family="Arial">
			<div class="jumbotron">
		        <div class="container">
		          <h1 class="display-3">Page d'Accueil</h1>
		          <p>Listes des potins créé par les utilisateurs</p>
		          <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more &raquo;</a></p>
		        </div>
		      </div>
			<div>
				<% @gossip.each do |t| %>
					<div class="row justify-content-center">
					 <div class="col-sm-6">
					   <div class="card">
					     <div class="card-body">
					       <h5 class="card-title"> Auteur : <%= @user.find(t.user_id).first_name%></h5>
					       <p class="card-text"><%= t.content %></p>
					       <a class="btn btn-primary" href="<%=gossip_path(t.id)%>">Info</a>
					       <a class="btn btn-primary" href="<%=edit_gossip_path(t.id)%>">Editer</a>
					     </div>
					   </div>
					 </div>
					</div>
					<br><br>
				<% end %>
			</div>
	</body>

</html>